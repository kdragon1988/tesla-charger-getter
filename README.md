# Tesla充電器Getter 🔌🚗

Tesla オンラインショップの在庫を常駐監視し、**在庫が復活した瞬間に「音・デスクトップ通知・日本語音声・商品ページ自動オープン」で知らせる** macOS 用ツールです。

人気の充電アクセサリ（Gen 2 モバイルコネクター等）は復活後 **数分で売り切れます**。このツールは在庫を **10〜20秒ごと** にチェックし、復活を逃さずお知らせします。

> 監視対象（既定）: **Gen 2 モバイルコネクター バンドル (JP)**
> 別の商品に変えることもできます（→「別の商品を監視する」）。

---

## ✨ 特徴

- **在庫API直叩き方式** — ページ全体を読み込み直すのではなく、Tesla の在庫API（`inventory.json`）を直接叩くため、軽量・高速・高頻度（10〜20秒）。取りこぼしを最小化します。
- **Akamai（bot対策）に強い** — あなたの実 Chrome のセッションを使うのでブロックされにくい。
- **普段の Chrome と共存** — 監視には専用プロファイルの Chrome を使うので、普段のブラウジングを邪魔しません。
- **派手なアラート** — 通知バナー＋システム音＋日本語音声を約1分間繰り返し＋商品ページを自動で開く。気づかないことはまずありません。

---

## 必要なもの

- **macOS**
- **Google Chrome**（[ダウンロード](https://www.google.com/chrome/)）
- **Node.js** v18 以上（[ダウンロード](https://nodejs.org/ja)：LTS版でOK）

---

## 🚀 クイックスタート（3ステップ）

### 1. ダウンロード

```bash
git clone https://github.com/kdragon1988/tesla-charger-getter.git
cd tesla-charger-getter
```

> Git を使わない場合は、GitHub ページの緑の「Code」→「Download ZIP」で取得し、解凍したフォルダで以下を実行してください。

### 2. セットアップ（初回だけ）

```bash
./setup.sh
```

Node.js の確認と必要なパッケージのインストールを自動で行います。

### 3. 監視スタート

```bash
./start.sh
```

- 監視専用の Chrome が自動で開き、在庫監視が始まります。
- **（推奨）開いた Chrome で一度 Tesla アカウントにログイン**しておくと、在庫復活時に**ワンクリックで購入**できます（このログインは初回だけ。専用プロファイルに保存されます）。
- 停止したいときは、ターミナルで **`Ctrl-C`**。

これだけです。あとは在庫復活を待つだけ。復活した瞬間に派手にお知らせします 🎉

---

## 在庫が復活すると何が起きる？

検知した瞬間、誤検知防止の確認チェックを1回行ってから、以下が**約1分間（3秒間隔×20回）**発動します：

1. 🔔 デスクトップ通知バナー「🚗 Tesla 在庫復活！」
2. 🔊 システムサウンドを繰り返し再生
3. 🗣️ 日本語の音声読み上げ「テスラの在庫が復活しました。今すぐ購入してください。」
4. 🌐 既定ブラウザで商品ページを自動で開く（ログイン済みならすぐ購入へ）

通知のテスト（鳴り方の確認）は以下で実行できます：

```bash
node drill-alert.mjs
```

---

## 別の商品を監視する

既定は「Gen 2 モバイルコネクター バンドル (JP)」です。別の充電器などを監視したい場合は、`src/config.js` の2か所を書き換えてください：

```js
const productUrl = "https://shop.tesla.com/ja_jp/product/＜商品スラッグ＞";
// ...
const inventory = Object.freeze({
  apiUrl: "https://shop.tesla.com/ja_jp/inventory.json",
  skuCode: "＜その商品の SKU＞", // ← ここを変更
});
```

**SKU の調べ方**：
1. Chrome で対象の商品ページを開く
2. 右クリック →「検証」→「Network」タブ
3. ページを再読み込みし、`inventory.json` のリクエストを探す
4. その「Payload（送信データ）」に `["1234567-00-A"]` のような **SKU** が入っています

---

## 仕組み（技術メモ）

- 監視には **CDP（Chrome DevTools Protocol）接続** を使います。Playwright が起動する自動ブラウザは Akamai に弾かれやすいため、`start.sh` が起動する**実 Chrome（専用プロファイル・デバッグポート9222）**に接続します。
- 在庫判定は **在庫API（`inventory.json`）を `POST ["SKU"]` で叩いた結果**で行います：
  - `purchasable: true` → **在庫あり**（`inventoryCount` は在庫ありでも 0 を返すため、`purchasable` を主軸に判定）
  - `purchasable: false` / `error: "Out of stock"` → 在庫切れ
  - 通信失敗・403・非JSON → UNKNOWN（バックオフして再接続）
- ページの reload はしません（初回にセッション確立のため1回だけ開く）。以降は軽量な API 呼び出しのみ。

---

## トラブルシューティング

### `Access Denied` / 403 が続く
Akamai に一時的にブロックされています。短時間に叩きすぎると起きます。
- ツールは自動でバックオフ＆再接続して回復を試みます。基本は放置でOK。
- 改善しない場合は、一度監視を止めて**数分〜十数分待ってから**再開してください。
- **Tesla にログイン**しておくと信頼されやすくブロックされにくくなります。

### 監視用 Chrome が起動しない
- Google Chrome がインストールされているか確認してください。
- 既にポート 9222 を別用途で使っている場合は、`REMOTE_DEBUG_PORT=9223 ./start.sh` のように変更し、`src/config.js` の `attach.cdpUrl` も合わせてください。

### 通知・音が出ない
- macOS の「システム設定 → 通知」でターミナル（または使用シェル）の通知を許可してください。
- 音量・出力先を確認してください。

### 監視用 Chrome のタブを閉じてしまった
- 自動で新しいタブを開いて回復します（数十秒）。気づいたら、監視専用 Chrome ウィンドウは**最小化**しておくと安全です。

---

## 停止・再開

- **停止**: 監視を実行中のターミナルで `Ctrl-C`。
- **再開**: もう一度 `./start.sh`。監視用 Chrome が起動済みならそのまま監視を再開します。

---

## ディレクトリ構成

```
tesla-charger-getter/
├── setup.sh            # 初回セットアップ（依存インストール）
├── start.sh            # 監視開始（Chrome起動 → 監視）★これを使う
├── run.sh              # 上級者向けランチャー（monitor.js に引数を渡す）
├── drill-alert.mjs     # 通知の動作テスト
├── test-judge.mjs      # 在庫判定ロジックの回帰テスト
├── src/
│   ├── config.js       # 設定（商品URL・SKU・間隔・アラート）
│   ├── monitor.js      # メインの監視ループ
│   ├── inventory.js    # 在庫API検知（判定ロジック）
│   ├── browser.js      # Chrome への CDP 接続
│   ├── notifier.js     # アラート（通知・音・音声・URLオープン）
│   ├── logger.js       # ログ出力
│   └── states.js       # 在庫状態の定数
├── package.json
├── LICENSE
└── README.md
```

---

## ⚠️ 免責事項

- 本ツールは**個人利用**を想定しています。**自己責任**でご利用ください。
- Tesla の利用規約を尊重し、**ポーリング間隔を極端に短くする等の過度なアクセスは行わないでください**（既定の10〜20秒は通常利用の範囲です）。サーバーに負荷をかける使い方や、転売目的での大量取得は避けてください。
- 在庫情報・購入の成否について、本ツールは一切の保証をしません。
- 本ツールは Tesla, Inc. とは無関係の非公式ツールです。

---

## ライセンス

[MIT License](./LICENSE)

---

🤝 後続の同志たちが少しでも入手しやすくなりますように。Good luck! 🚗⚡
